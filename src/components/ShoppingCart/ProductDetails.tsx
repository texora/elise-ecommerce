import { Flex } from '@chakra-ui/react'

export const ProductDetails = ({ id }: { id: number }) => {
  return (
    <Flex maxW={{ base: '100%', '2xl': '75%' }} justifyContent='center' alignContent={'center'}>
      45
    </Flex>
  )
}
